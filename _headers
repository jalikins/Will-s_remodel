/*
  # UPDATED Content-Security-Policy to allow all necessary sources
  Content-Security-Policy: default-src 'self'; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com; script-src 'self' https://cdn.jsdelivr.net https://code.jquery.com https://cdnjs.cloudflare.com https://maps.googleapis.com; img-src 'self' data: https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://maps.gstatic.com *.googleapis.com *.ggpht.com; font-src 'self' https://cdn.jsdelivr.net; frame-src 'self' https://www.google.com; frame-ancestors 'self';

  # X-Content-Type-Options: Prevents the browser from trying to guess the content type of a file.
  X-Content-Type-Options: nosniff

  # Referrer-Policy: Controls how much information is sent when a user clicks a link to another site.
  Referrer-Policy: strict-origin-when-cross-origin
  
  # Permissions-Policy: Controls which browser features can be used on your site.
  Permissions-Policy: midi=(), sync-xhr=(), microphone=(), camera=(), magnetometer=(), gyroscope=(), fullscreen=(), payment=()